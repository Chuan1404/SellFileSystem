import { Typography } from "@mui/material";
import { Authenticated } from "../components";

export default function Mail() {

  return (
    <Authenticated>
      <Typography variant="h1">Mail</Typography>
    </Authenticated>
  );
}
