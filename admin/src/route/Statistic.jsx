import { Typography } from "@mui/material";
import { Authenticated } from "../components";

export default function Statistic() {

  return (
    <Authenticated>
      <Typography variant="h1">Statistic</Typography>
    </Authenticated>
  );
}
